package com.jdbc;

import java.sql.Connection;
import java.sql.Driver;
import java.sql.DriverManager;
import java.sql.Statement;

public class FirstProgram {

	public static void main(String args[]){/*
		try {
			// InterfaceI ref = new ClassA();
			//1. Load the driver
			Driver d = new com.mysql.jdbc.Driver();
			DriverManager.registerDriver(d);

			//Class.forName("com.mysql.jdbc.Driver");
			
			//2. Obtain connection
			Connection con = DriverManager.getConnection("jdbc:mysql://localhost/test", "root", "root");

			//3. Create a Statement
			Statement ps = con.createStatement();
			
			//4. Execute the Statement 
			//int count = ps.executeUpdate("insert into student values(3,'ms','rajababu')");

			//int count = ps.executeUpdate("update student set program='commerce' where studentId=3");
			
			int count = ps.executeUpdate("delete from student where studentId=3");
			
			//int count = ps.executeUpdate();
			System.out.println(count+" row(s) updated succefully..");
			
			//5. Closing the connection
			con.close();
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
		}

	
*/}
}
